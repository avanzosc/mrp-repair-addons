<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_location_form" model="ir.ui.view">
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form" />
        <field name="arch" type="xml">
            <field name="replenish_location" position="after">
                <field name="eligible_for_repairs" />
            </field>
        </field>
    </record>

    <record id="view_location_tree2" model="ir.ui.view">
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_tree2" />
        <field name="arch" type="xml">
            <field name="storage_category_id" position="after">
                <field name="eligible_for_repairs" optional="show" />
            </field>
        </field>
    </record>

    <record id="view_location_search" model="ir.ui.view">
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_search" />
        <field name="arch" type="xml">
            <field name="location_id" position="after">
                <field name="eligible_for_repairs" />
            </field>
            <filter name="inactive" position="after">
                <filter
          string="Eligible for repairs"
          name="eligible-for-repairs"
          domain="[('eligible_for_repairs','=',True)]"
        />
            </filter>
        </field>
    </record>
 </odoo>
